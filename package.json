{
  "name": "agentic-cockpit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "File-backed agent bus + Codex workers + operator cockpit (tmux + dashboard).",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dashboard": "node scripts/dashboard/server.mjs",
    "test": "node --test",
    "lint": "node -e \"console.log('no linter configured')\""
  }
}
